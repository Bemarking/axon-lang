---
title: "Plan vivo: Fase 35 — axonstore as a cognitive data plane (the persistent relation no other language has)"
status: ✅ CLOSED — D1–D14 ratificadas bloque 2026-05-15 (founder "D-letters todos ratificados"). Reframed from the industry-standard port to the axon-native vision per founder directive. Triggered by the kivi-enterprise gap report 2026-05-15, verified true; reframed because a faithful Postgres-ORM port would only EQUAL the market, and axon exists to surpass it. **ALL 14 sub-fases 35.a–35.n SHIPPED** — anchor (5) + filter compiler (53) + `PostgresStoreBackend` (37) + `StoreRegistry` (26) + sync-runner (6) + streaming-dispatcher (7) + **Pillar I epistemic (18)** + **Pillar II audit-chain (19)** + **Pillar III `Stream<Row>` (11)** + **Pillar IV capability-typed access (15)** + **D13 fuzz (~24 000 iters)** + **D14 real-Postgres integration (12 tests, verified vs a live `postgres:16`)** + **adopter docs + `mutate`/`purge` `where`-parser gap closed (3 tests)**; 2011 axon-rs + 253 axon-frontend lib tests, zero regressions. **35.n SHIPPED 2026-05-16** — axon-lang v1.30.0 live cross-stack (crates.io `axon-lang` 1.30.0 + `axon-frontend` 0.13.0 + PyPI 1.30.0 + GitHub Release v1.30.0 + `rust_release.yml` multi-arch binaries) and axon-enterprise v1.21.0 catch-up live (PR #26 commit `2a3ab27` + tag `v1.21.0` + GitHub Release; the PR also fixed a nightly-toolchain `-Zsanitizer` doctest ABI-mismatch CI breakage via `RUSTDOCFLAGS`). **35.o SHIPPED 2026-05-16** — post-release adopter-gap follow-up: the `persist` field-block parser gap (the third store-op, the SAME class of gap 35.m closed for `mutate`/`purge`); `persist <store> { col: value }` now honored end-to-end; ships as **axon-lang v1.31.0** + axon-enterprise v1.22.0 catch-up. **35.p SHIPPED 2026-05-16** — `mutate` SET-field-block: the symmetric gap to 35.o (`mutate` built its `UPDATE … SET` from every binding → failed against any real table). `parse_mutate_step` captures `where:` + `col: value` SET assignments; ships as **axon-lang v1.32.0** + axon-enterprise v1.23.0 catch-up. **🎯 Fase 35 — 16 sub-fases 35.a–35.p; the cognitive data plane is live and all three write-ops (`persist`/`mutate`/`purge`) + `retrieve` scope their columns — usable in real flows.**
owner: AXON Runtime + Backends Team
created: 2026-05-15
target: axon-lang v1.30.0 (minor — `axonstore` becomes a load-bearing runtime primitive: a persistent relation that is epistemically typed, audit-chained by construction, streamable, and capability-secured)
depends_on: Fase 34 SHIPPED v1.29.x (the `unified_stream_handler` + `Stream<ToolChunk>` surface — Pillar III routes through it). Fase 33.z SHIPPED v1.27.0 (the dispatcher is the production path). ESK Fase 6 (the epistemic lattice — Pillar I). Fase 16 / `esk::provenance` (HMAC-Merkle audit chains — Pillar II). Fase 32.g `auth_scope` + Fase 11/13 trust + capability types (Pillar IV). `sqlx` (postgres) + `axon-rs/src/db_pool.rs` already establish the `PgPool` substrate.
charter_class: SPLIT — the cognitive data plane primitive (the four pillars) is **OSS, non-negotiably** — it is the `axonstore` joining systems axon-lang OSS already ships; walling it would amputate the language. The **moat is the hardening layer, not the primitive**: the enterprise product is the named **axonstore Regulatory Hardening Layer** (FIPS-validated audit-chain crypto + mmap tamper-evident kernel + court-admissible evidence packager + per-vertical confidence policies + multitenant store-isolation ops + per-vertical regulatory replay). Per-sub-fase classification + the full enterprise surface are in §6. v1.30.0 ships the OSS plane; the Hardening Layer ships on the axon-enterprise vertical R&D track (the Fase-20 shape).
strategic_direction: This cycle is built **Rust-canonical**. Per the founder directive 2026-05-15 — *"todo encaminado a ser 100% Rust + C, 0 Python"* — the Rust implementation IS the canonical `axonstore` runtime. The Python `axon/runtime/store_backends/` modules are the historical reference this cycle learns from, but they are frozen: no new Python store work, no Python↔Rust parity infrastructure is built (a permanent cross-stack drift gate would deepen exactly the coupling the project is retiring). The Python store backends are on the eventual deprecation path.

pillars: |
  MATHEMATICS — A relation `R` is not just a set of tuples. axon's `axonstore` is a relation enriched, ORTHOGONALLY, in four dimensions the relational model never carried: (1) an EPISTEMIC grading — each tuple sits somewhere in the lattice ⊥ ⊑ doubt ⊑ speculate ⊑ believe ⊑ know; (2) a TEMPORAL-INTEGRITY structure — the mutation history is a Merkle-chained sequence, a free monoid of deltas with a verifiable fold; (3) an ALGEBRAIC-EFFECT structure — selection σ_φ(R) is a coinductive `Stream`, not an eager set; (4) a CAPABILITY structure — access to `R` is a typed permission in the linear/affine sense. The faithful-ORM port realizes only the bare relation. Fase 35 realizes the enriched object — which is a genuinely new mathematical structure, not present in SQLAlchemy / Prisma / Diesel / sqlx, because those have no epistemic lattice, no algebraic-effect calculus, no capability types to enrich WITH.

  LOGIC — The four enrichments are not bolt-ons; they are axon SYSTEMS the store JOINS. axon already HAS the epistemic lattice (ESK, Fase 6), the HMAC-Merkle audit chain (Fase 16 / `esk::provenance`), the algebraic-effect streaming runtime (`unified_stream_handler`, Fase 34), and capability/trust types (Fase 11/13/32). No new invention is required — the `axonstore` simply becomes the PERSISTENT-DATA member of each system. The reason no other language can copy this is not cleverness; it is that no other language has the four systems to join.

  PHILOSOPHY — The language honors its own declarations — ALL of them. `IRAxonStore` carries `confidence_floor` and `on_breach` fields that the runtime has always ignored, exactly as it ignored `backend`. A Fase 35 that honored `backend` while leaving `confidence_floor`/`on_breach` inert would close one instance of the defect and ship the next. The reframed Fase 35 honors every field the declaration carries — and makes the declaration MEAN something the market cannot offer.

  COMPUTING — axon's purpose is to be the default language for building AI applications and agents. An agent's persistent memory is the substrate of its cognition: what it believes, how sure it is, what it is allowed to read, and a tamper-evident record of how its world changed. A plain table answers none of those. The cognitive data plane answers all four — as language primitives, verified, with zero adopter bolt-on. That is the floor for "the default language for AI software".

---

## ▶ 1. The trigger + the reframe

The kivi-enterprise adopter filed a gap report 2026-05-15: the Rust runtime ignores `axonstore { backend: postgresql }`. **Verified true** (file-by-file; the gap is in BOTH the sync runner `runner.rs` AND the streaming dispatcher `flow_dispatcher/wire_integrations.rs` — both route `retrieve`/`persist`/`mutate`/`purge` to a key-value store, never SQL). The Python runtime has a `PostgreSQLStoreBackend` reference; the Rust runtime has nothing.

The first draft of this plan proposed the obvious fix: a faithful Rust port of the Python backend — `PgPool` + parameterized CRUD. **That draft was correct and would have closed the gap — and it would only have made axon EQUAL the market.** "Map a store declaration to a table and do CRUD with a parameterized WHERE" is what every ORM does.

The reframe: `axonstore` is not a table. The proof is in the language's own IR — `IRAxonStore` carries `confidence_floor: Option<f64>` and `on_breach: String`, fields the language designers placed there and the runtime never honored. They are the language saying, in its own type, that a store was always meant to be a *cognitive* object. Fase 35 builds that object.

## ▶ 2. The model — the four enrichments

A normal store is `Relation`. The axon `axonstore` is `Relation` enriched in four orthogonal dimensions, each by JOINING an axon system that already exists:

### Pillar I — Epistemic (the store joins the ESK lattice)

Every tuple in an `axonstore` has an epistemic grade. Data does not enter the world `true` — it enters `Untrusted` (⊥) and is *elevated* by reasoning. This is already how ℰMCP-sourced data (`EpistemicTaint`) and LLM-streamed tokens (`EpistemicGradient`) are treated. The `axonstore` joins that discipline:

- A row produced by `retrieve from S` is born `Untrusted` — a downstream `shield` / `know` / reasoning step must elevate it before it is trusted. A `retrieve` result is NOT a fact; it is a claim.
- `confidence_floor: f` on the store is enforced: `retrieve` filters or flags tuples whose stored confidence is below `f`.
- `persist into S` of a value below the store's `confidence_floor`, or of an un-elevated `Untrusted` value, is a typed error — you cannot quietly write doubt into a believed store.

No ORM has the concept "this row has a confidence and a trust level". axon does — the store joins it.

### Pillar II — Audit-chained by construction (the store joins the Fase 16 / ESK provenance chain)

Every `persist` / `mutate` / `purge` appends a delta to a tamper-evident, **HMAC-Merkle-chained** mutation log. The chain's crypto is the OSS `sha2`/`hmac` path (axon-enterprise overrides with the FIPS-validated + mmap kernel from `axon-csys-enterprise`, per the SPLIT charter — and the C kernels are the "Rust + C" half of the stack).

- The store's complete mutation history is an independently verifiable Merkle sequence — regulatory replay (PCI DSS Req 10, FedRAMP AU-2, 21 CFR Part 11 §11.10, FRE 502) as a *language primitive*, not an event-sourcing framework the adopter bolts on.
- `on_breach: String` is honored: when chain verification detects tampering, the declared policy fires (`halt` / `quarantine` / `alert`).

The market answer is CDC + audit triggers + an event store, integrated by hand. axon's `axonstore` gives a tamper-evident mutation chain for free.

### Pillar III — `retrieve` is a `Stream<Row>` (the store joins the Fase 34 algebraic-effect surface)

`retrieve from S where φ` is the coinductive selection — a `Stream<Row>` drained through the **same `unified_stream_handler` Fase 34 shipped**. A large result set is not materialized; rows flow with a declared `BackpressurePolicy`, cancel-aware (D5 cancel-into-body).

- A pg-backed `axonstore` is, structurally, a first-class stream producer — unified with `Tool::stream()` and the four streaming-effect disjunctions.
- `retrieve from huge_table` does not OOM the agent; it streams, exactly like an LLM token stream, through one drain loop.

The market answer is cursors and manual pagination. axon's is the algebraic-effect stream it already has.

### Pillar IV — Capability-typed access (the store joins the trust / capability type system)

`retrieve` / `persist` / `mutate` / `purge` against `S` require the executing flow to hold a capability for `S`. The **type-checker enforces it** — store access is a typed permission, not an app-code `if tenant_id == …` the developer must remember.

- Data isolation (the tenant-isolation audit found it lives entirely in app code) becomes a *language guarantee*: a flow without the capability cannot, by construction, read the store.
- This joins Fase 32.g auth scopes, Fase 11/13 trust types + capability extrusion.

The market answer is row-level-security policies + app-layer scoping. axon's is a capability type the compiler checks.

## ▶ 3. Why this is the market-surpassing object

| Concern | Industry standard (ORM / query builder) | axon `axonstore` (Fase 35) |
|---|---|---|
| A retrieved row is… | a fact | a *claim* — born `Untrusted`, must be elevated (Pillar I) |
| Mutation history | bolt-on: CDC / triggers / event store | a verifiable HMAC-Merkle chain, by construction (Pillar II) |
| Large result sets | manual cursors / pagination | a backpressured, cancel-aware `Stream<Row>` (Pillar III) |
| Access control | RLS policies + app-layer `tenant_id` checks | a capability the type-checker enforces (Pillar IV) |
| Why others can't copy it | — | they have no epistemic lattice, no algebraic-effect calculus, no audit-chain primitive, no capability types to enrich a relation WITH |

## ▶ 4. D-letters proposed (D1–D14) — pending founder bloque ratification

**The substrate (D1–D7) — the relation must be real before it can be enriched:**

- **D1 — `axonstore { backend: postgresql }` honored at runtime.** `retrieve`/`persist`/`mutate`/`purge` against a postgresql-backed store execute real SQL against the declared `connection` — not the key-value path.

- **D2 — Store resolution is a total function over a closed catalog.** Every store-op IR node's `store_name` resolves against `IRProgram.axonstore_specs`. `backend ∈ {in_memory, postgresql}` (closed; `in_memory` is the implicit default). Unknown backend or unresolvable store → a named error, never a silent KV lookup.

- **D3 — Zero regression on the key-value path (absolute).** A flow using only in-memory/default stores behaves byte-identically to pre-35 on BOTH execution paths. The SQL path is entered iff a matching `IRAxonStore` has `backend == "postgresql"`. kivi's explicit acceptance criterion; non-negotiable.

- **D4 — Every WHERE clause is parameterized — SQL-injection-proof by construction.** `where "<expr>"` parses into a closed-catalog `FilterCondition` AST (column regex `[a-zA-Z_]\w*`; operators whitelisted `{=,!=,>,>=,<,<=,LIKE}`; values typed) and renders with `$N` bind placeholders. No code path interpolates a user value into SQL.

- **D5 — Both execution paths honored identically.** The sync runner (`runner.rs`) AND the streaming dispatcher (`flow_dispatcher::wire_integrations`) route postgresql-backed ops through the SAME backend. No path divergence — the SSE-gap lesson is not repeated.

- **D6 — Connection resolution: `connection: "env:VAR"` + literal DSN.** `env:`-prefixed values resolve the named environment variable; other values are literal DSNs. Missing env var → a clear named error, never a panic, never a silent KV fallback.

- **D7 — Pooling + honest typed failure surface.** One `sqlx::PgPool` per distinct resolved DSN, lazy, bounded, reused. Every failure (connect, auth, missing table, SQL error, malformed where-expr, type mapping) → a typed named error. No panic; no silent empty result masking a failed query.

**The four enrichments (D8–D11) — the axon-native object:**

- **D8 — Pillar I, Epistemic data plane.** Every tuple from `retrieve` is born `Untrusted` (⊥) in the ESK lattice. `confidence_floor` is enforced at `retrieve` (sub-floor tuples filtered/flagged) and at `persist` (writing an un-elevated or sub-floor value is a typed error). The `axonstore` is a participant in axon's epistemic discipline, not an opaque byte store.

- **D9 — Pillar II, Audit-chained by construction.** Every `persist`/`mutate`/`purge` appends a delta to a tamper-evident HMAC-Merkle mutation chain (OSS `sha2`/`hmac` crypto; enterprise overrides with the FIPS + mmap C kernel). The chain is independently verifiable. `on_breach` is honored on tamper detection. Regulatory replay is a primitive, not a framework.

- **D10 — Pillar III, `retrieve` is a `Stream<Row>`.** Result sets drain through the Fase 34 `unified_stream_handler` with a `BackpressurePolicy`, cancel-aware. A pg-backed `axonstore` is a first-class stream producer, unified with the algebraic-effect surface — large `retrieve` never materializes eagerly.

- **D11 — Pillar IV, Capability-typed store access.** `retrieve`/`persist`/`mutate`/`purge` against `S` require a capability for `S`, enforced by the type-checker (compile-time) and re-checked at runtime. Data isolation becomes a language guarantee. Joins Fase 32.g auth scopes + Fase 11/13 trust/capability types.

**Honest scope + robustness (D12–D14):**

- **D12 — Schema-absence + transaction scope are honest, documented boundaries.** `IRAxonStore` carries no column schema → v1.30.0 operates against existing tables (no `CREATE TABLE`/`migrate`/`CREATE INDEX` DDL — needs an IR schema extension, a documented follow-on). Each op is single-statement autocommit; the multi-statement `transact { … }` block is a documented future fase. Boundaries are stated in the adopter docs, not silently omitted.

- **D13 — Production-grade D12 fuzz.** `axon-rs/tests/fase35_fuzz.rs` (hand-rolled LCG): filter compiler totality + never-panic; SQL-injection-resistance as a fuzzed invariant (no input → unparameterized value); closed-catalog operator/column rejection totality; epistemic-grade assignment totality; audit-chain integrity under arbitrary delta sequences; store-resolution totality.

- **D14 — Real-Postgres integration tests (the robustness floor).** `axon-rs/tests/fase35_*_postgres_integration.rs` exercises all four pillars end-to-end against a REAL Postgres (Docker container spun up by the harness): real rows, real `confidence_floor` filtering, real audit-chain Merkle verification, real `Stream<Row>` backpressure, real capability gating. Not mocks. The implementation is proven against an actual database before it ships.

## ▶ 5. Sub-fase shape — sequenced execution

Substrate first (35.a–f) — the relation must be real before it is enriched. Then the four pillars (35.g–j), each joining an existing axon system. Then the robustness gates (35.k–l), docs (35.m), release (35.n).

| Sub-phase | Scope | LOC target | Status | Description |
|---|---|---|---|---|
| **35.a** | Diagnostic anchor + four-pillar architecture spec | ~430 | ✅ SHIPPED | `axon-rs/tests/fase35_a_axonstore_gap_diagnostic.rs` (5 tests, all green) pins the v1.29.x baseline: §1 — the frontend lowers `axonstore { backend: postgresql … }` into a full `IRAxonStore` spec (`backend`/`connection`/`confidence_floor`/`isolation`/`on_breach` all preserved through lexer→parser→IR); §2 — the four store helpers route unconditionally to in-memory KV (`__store_<name>_<key>`), backend-blind (no `IRAxonStore` in any signature); §3 — `run_persist`/`run_retrieve` round-trip through `let_bindings`, the IR step nodes carry no backend handle and `DispatchCtx` has no SQL pool — the spec and the steps are structurally disconnected; §4 — the four pillars (epistemic / audit-chain / streaming / capability) are all absent today; §5 — closed-catalog totality pins (4 store ops + 4 pillars). Each subsequent sub-fase inverts a specific §-assertion. Founder D1–D14 ratification. |
| **35.b** | Filter compiler — `where`-expr → parameterized SQL | ~900 | ✅ SHIPPED | New `axon-rs/src/store/` module (`mod.rs` + `filter.rs`, registered in `lib.rs`). `filter.rs` ships the total filter compiler: a lexical tokenizer (closed alphabet — identifiers / comparison symbols / quoted strings with escape resolution / signed-decimal numbers) → recursive parser → typed `Filter` AST (`FilterCondition` = `column · Operator · SqlValue`, joined by a closed `Connector` catalog) → `build_pg_where(expr, offset) -> Result<(String, Vec<SqlValue>), FilterError>` with `$N` placeholders. **D4 invariant**: no code path interpolates a user value into SQL — every value is a bind placeholder, every column is double-quoted (and the `[A-Za-z_]\w*` lexer guarantees no embedded quote). Stricter than the frozen Python reference — the dangling-connector defect (`"id = 1 AND"` → broken SQL) is a typed `DanglingConnector` error. Pure, no I/O. `NULL` folds to `IS NULL`/`IS NOT NULL` (consumes no placeholder). DoS guards: 63-byte column limit + 256-condition cap. 15-variant typed `FilterError` (D7 honest failure surface). 50 exhaustive unit tests (every value type, every operator + aliases, NULL folding, param-offset, 4 injection-resistance tests, escapes, all lexical/structural/semantic errors, bounds). 1873 axon-rs lib tests green, zero regressions. D4. |
| **35.c** | `PostgresStoreBackend` — SQL substrate | ~1000 | ✅ SHIPPED | New `axon-rs/src/store/postgres_backend.rs` (registered in `store/mod.rs`). `PostgresStoreBackend` holds one lazy, bounded `sqlx::PgPool` (`connect_lazy` — no connection until first op) and exposes async `query`/`insert`/`mutate`/`purge`/`ping`. **D6**: `resolve_dsn` honors `connection: "env:VAR"` (env-var resolution) + literal DSN; a missing var is a typed `StoreError::MissingEnvVar`, never a panic, never a silent KV fallback. **D7**: 11-variant typed `StoreError` (honest failure surface — empty/missing connection, malformed DSN, invalid identifier, SQL error, unsupported column type, decode failure); no panic, no silent empty-result masking. **D4 extended to identifiers**: table + insert/mutate column names validated via `filter::is_safe_identifier` before double-quoting (no untrusted identifier reaches SQL); values flow through 35.b's `build_pg_where` as `$N` placeholders; `NULL` renders inline (injection-safe keyword, no placeholder). Architecture = **pure SQL builders** (`build_select_sql`/`build_insert_sql`/`build_update_sql`/`build_delete_sql` — no I/O, total) + thin async execution. `build_update_sql` fixes the Python reference's offset bug — WHERE `$N` numbering continues after the *non-NULL* SET count, not the column count. pg-row → JSON-safe mapping: `classify_pg_type` (closed 15-class catalog — bool/int2-4-8/float4-8/numeric/text/uuid/timestamp(tz)/date/time/json(b)/bytea) → `StoreRow`; UUID→string, TIMESTAMPTZ→RFC3339, NUMERIC→string (precision-safe), BYTEA→base64 — pre-empting the kivi-reported Python JSON-encoder monkey-patches. DSN password masked in `Debug` + errors. sqlx `uuid`+`bigdecimal` features added. 37 exhaustive unit tests (pure surface — DSN resolution, connect/masking, all 4 SQL builders, injection resistance on values + identifiers, type classification, `StoreRow`, every error Display); live execution + row decode proven in 35.l. 1913 axon-rs lib tests green, zero regressions. D6 + D7. |
| **35.d** | `StoreRegistry` + closed-catalog dispatch | ~700 | ✅ SHIPPED | New `axon-rs/src/store/registry.rs` (registered in `store/mod.rs`) — the single SQL-vs-KV chokepoint both execution paths route through. **D2**: `StoreRegistry::build(&[IRAxonStore])` is the closed-catalog gate — `classify_backend` maps `backend` into `{InMemory, Postgresql}` (trimmed, case-insensitive; empty = implicit default); an unknown backend (`sqlite`/`mysql`/typo) or a duplicate store name fails the build with a typed `RegistryError` — pure, no I/O, fail-fast at deploy. `resolve(store_name)` is then total → `StoreHandle` or `StoreError`, never a panic. **D3 (absolute)**: an undeclared store, or one declared `in_memory`/empty, resolves to `StoreHandle::InMemory` — the byte-identical pre-35 key-value path; the SQL path is entered iff a matching `IRAxonStore` has `backend == "postgresql"`. A declared `postgresql` store whose connection cannot be resolved (missing `env:` var, malformed DSN, empty connection) yields a typed error — **never a silent KV fallback** (silently degrading would lose writes / serve stale reads). **D7**: lazy per-DSN pool cache — a `PostgresStoreBackend` (and its `env:` resolution) is created on first `resolve` of a postgresql store and cached by resolved DSN, so stores sharing a DSN share one pool, and a broken postgresql store an `in_memory` flow never touches cannot break that flow (the build stays pure). `Debug` lists store names + kinds only (never raw `connection` — a literal DSN can carry a password). 26 exhaustive unit tests (catalog classification, build gate, duplicate rejection, D3 in-memory resolution, D2 no-silent-fallback, per-DSN cache dedup + distinct-pool, malformed-DSN-not-cached, accessors, Debug-no-leak). 1939 axon-rs lib tests green, zero regressions. D2 + D3. |
| **35.e** | Wire into the sync runner | ~400 | ✅ SHIPPED | `runner.rs` — `execute_server_flow` (server) + `run_run` (CLI) build the `StoreRegistry` from `IRProgram.axonstore_specs` (D2 gate fires here — an unknown backend fails fast at deploy) + thread it into `execute_real`. The session-memory interception gains an **additive** SQL pre-branch: a `persist`/`retrieve`/`mutate`/`purge` whose store resolves to `Postgresql` executes real SQL via `PostgresStoreBackend` and `continue`s; `remember`/`recall` and every `in_memory`/undeclared store fall through to the **byte-identical pre-35 key-value path** — the existing KV `match` is not touched at all (D3 absolute, structural guarantee). Sync→async bridge: `block_on_store` runs the async store op on a freshly-spawned OS thread that owns a current-thread Tokio runtime — no "runtime within a runtime" hazard whether `execute_real` runs on a server worker thread, a `spawn_blocking` thread, or a plain CLI thread. Store name = SQL table (D12 — no schema); `retrieve`/`purge` driven by the `where`-expr; `persist`/`mutate` write the flow's user bindings as a row (new `ExecContext::user_bindings` — `let`-bindings + step results, runner built-ins excluded). D5 — the SAME `PostgresStoreBackend` the streaming dispatcher uses, no path divergence. A failed SQL op surfaces as a typed `store error:` step result — never a panic, never a silent KV fallback. 6 tests (`user_bindings` ×2, `block_on_store` from a plain thread + from inside a runtime, `execute_sql_store_step` missing-env-var + persist-bindings-as-row); 13 persist + 9 axonstore + fase33z parity/fuzz integration suites green. 1945 axon-rs lib tests green, zero regressions. Live SQL execution proven in 35.l. D3 + D5. |
| **35.f** | Wire into the streaming dispatcher | ~400 | ✅ SHIPPED | `DispatchCtx` gains a `store_registry: Option<Arc<StoreRegistry>>` field + `with_store_registry` builder (mirrors the `tool_registry`/`pem_backend` opt-in pattern); `streaming_via_dispatcher` builds the registry from `IRProgram.axonstore_specs` right after IR compilation (D2 gate — an unknown backend emits a named `FlowError` + `FlowComplete` before any node dispatches) and attaches it. `wire_integrations.rs` — `run_persist`/`run_retrieve`/`run_mutate`/`run_purge` gain an **additive** SQL pre-branch via `resolve_pg_backend(ctx, store_name)`: `Ok(Some(backend))` → real SQL through `PostgresStoreBackend`; `Ok(None)` → the **byte-identical** pre-35 key-value helper (`persist_to_store`/… — unchanged, reproduced verbatim in the `Ok(None)` arm); `Err` → `DispatchError::BackendError`, never a silent KV fallback (D2). When no registry is attached (`DispatchCtx::new` default) every op is key-value — D3 absolute, structural. The dispatcher is async, so no sync→async bridge is needed; `resolve` uses the registry's **durable per-DSN pool cache** (the server runtime is long-lived) — concurrent branches share pools (the production hot path, unlike 35.e's per-op connect). `persist`/`mutate` write the flow's `let_bindings` as a row (`sql_row_from_bindings` — `__`-prefixed namespace keys excluded, sorted); `retrieve`/`purge` driven by `where_expr`. D5 — the SAME `PostgresStoreBackend` the sync runner uses (35.e), no path divergence. 7 tests (`resolve_pg_backend` no-registry/in-memory/missing-env, `run_retrieve`+`run_persist` postgresql → typed `BackendError`, `run_persist` in_memory → byte-identical KV path, `sql_row_from_bindings`); the 19 existing wire-integration handler tests now exercise the `Ok(None)` KV arm = D3 proven. 1952 axon-rs lib tests + fase33z/34 dispatcher suites green, zero regressions. Live SQL execution proven in 35.l. D3 + D5. |
| **35.g** | **Pillar I — Epistemic data plane** | ~850 | ✅ SHIPPED | New `axon-rs/src/store/epistemic.rs` (registered in `store/mod.rs`) — the pure, total epistemic-policy module joining the `axonstore` to the ESK trust model. **Born Untrusted**: `mark_retrieved` tags every retrieved tuple `EpistemicTaint::Untrusted` (⊥) — joins the existing ℰMCP taint lattice, no new type invented; `retrieve` now returns an **epistemic envelope** (`{taint, confidence_floor, trusted_rows, below_floor_filtered, rows}`) so a retrieved claim cannot be mistaken for a fact. **`confidence_floor` at retrieve**: `enforce_retrieve_floor` partitions rows — a tuple's stored confidence (read from the reserved `_confidence` column — D12-compatible, no DDL; open-question #2 resolved to the column convention) below the floor is filtered from the trusted result, the dropped count surfaced (never silent). A row with no `_confidence` is at ⊥ → below any positive floor. **`confidence_floor` at persist**: `enforce_persist_floor` — a `persist` into a floored store of a sub-floor value, an un-elevated value (no `_confidence`), or a malformed `_confidence` is a typed `EpistemicError` (3 variants) — you cannot quietly write doubt into a believed store. Wired into BOTH execution paths (D5): 35.e `runner.rs` `execute_sql_store_step` + 35.f `wire_integrations.rs` `run_persist`/`run_retrieve` — `resolve_pg_backend` now returns `(backend, confidence_floor)`. New `StoreError::Epistemic(EpistemicError)` variant + `From`. **SPLIT** (§6): this is the OSS mechanism (numeric floor, `≥` enforcement); the enterprise seam is the plain `f64` — vertical confidence calibration layers on top unchanged. mutate is not floor-checked (D8 scopes enforcement to `persist`). 18 tests (16 `epistemic.rs` pure-policy — confidence extraction from JSON/SQL, born-Untrusted, floor partitioning, no-confidence-below-floor, envelope shape, all 3 persist-error variants, every error Display; + 2 wiring — sub-floor persist blocked end-to-end through the runner AND the dispatcher before any row is written). 1970 axon-rs lib + persist/fuzz integration suites green, zero regressions. D8. |
| **35.h** | **Pillar II — Audit-chained mutations** | ~750 | ✅ SHIPPED | New `axon-rs/src/store/audit_chain.rs` (registered in `store/mod.rs`) — the store-domain layer over Fase 6.2's HMAC-SHA256 Merkle chain (`esk::provenance::ProvenanceChain` — the chain crypto is **joined, not reinvented**). `StoreAuditChain` retains each delta payload + wraps a `ProvenanceChain<HmacSigner>`; `record(kind, store, summary)` appends a Merkle-linked, HMAC-signed delta (`seq`+`op`+`store`+`summary`+`timestamp` hashed — `seq` keeps identical mutations distinct); `head()` is the tamper-evident Merkle root of the flow's whole mutation history; `verify()` re-hashes + re-links every delta → `ChainVerdict::{Intact, Tampered}`. **`on_breach`** is a closed-catalog `OnBreachPolicy::{Log, Raise, Rollback}` — `resolve_on_breach` over the existing `VALID_STORE_ON_BREACH` set (§7 question #3 resolved: the catalog is `{log, raise, rollback}`, not a new one; empty → `Log`); `apply_on_breach`/`audit` fire the policy on a detected tamper → `BreachOutcome` (`Clean`/`Logged`/`Raised`/`RolledBack`, `is_halting`). Wired into the streaming dispatcher: `DispatchCtx` gains an `Arc<Mutex<StoreAuditChain>>` (shared across `Par` branches → one chain); `run_persist`/`run_mutate`/`run_purge` append a delta after a successful op (`record_store_mutation`), `run_retrieve` does not (D9 scopes the chain to mutations). **SPLIT** (§6): OSS = the HMAC-Merkle mechanism + `sha2`/`hmac` crypto + `on_breach` (in-process, flow-lifetime). ENTERPRISE = the FIPS-validated crypto link + the mmap tamper-evident persistent kernel + the court-admissible evidence packager; the seam is the `esk::provenance::Signer` trait + the in-memory chain. 19 tests (16 `audit_chain.rs` — mutation-kind rendering, `on_breach` resolution, chain append + head advance + delta linkage, intact/empty verification, tamper-of-summary + tamper-of-op detection, all 3 policy outcomes, `is_halting`, `audit` composition, Debug-no-leak; + 3 dispatcher wiring — persist appends a verifiable delta, retrieve appends none, persist+mutate+purge chain into one verified history). 2000 axon-rs lib + dispatcher integration suites green, zero regressions. D9. |
| **35.i** | **Pillar III — `retrieve` as `Stream<Row>`** | ~600 | ✅ SHIPPED | New `axon-rs/src/store/row_stream.rs` (registered in `store/mod.rs`) — `retrieve` becomes the coinductive selection σ_φ(S): rows flow off a **lazy `sqlx` cursor** (`.fetch()` — the full result set is never materialized) through a bounded, cancel-aware drain. `stream_retrieve` opens the cursor over `build_select_sql`'s parameterized SELECT and drains it; `drain_with_policy` is generic over any row stream so the policy + cancel logic is exhaustively unit-testable with synthetic streams (no DB). **Joins the Fase 34 streaming surface** — reuses the closed `BackpressurePolicy` catalog (`drop_oldest` keeps a bounded tail / `fail` errors past the bound / `pause_upstream` truncates + flags / `degrade_quality` = the OSS identity degrader) + the `CancellationFlag` cancel discipline (a DB row is not a `ToolChunk` — no token text, no SHA-256, no wire terminator — so the drain is row-shaped, joining the streaming *model* rather than literally the token handler). `retrieve from huge_table` stays inside a `DEFAULT_MAX_ROWS` (10 000) bound — no OOM. Wired into BOTH paths (D5): the streaming dispatcher `run_retrieve` (cancel-driven via `ctx.cancel`) and the sync runner `execute_sql_store_step`; the epistemic envelope (35.g) gains a `"stream"` sub-object (`policy`/`total_seen`/`dropped`/`truncated`/`cancelled`). `map_pg_row` + `bind_value` lifted to `pub(crate)` so the cursor drain shares one row-decode + bind path with `query`. **OSS** (§6) — the lazy cursor + closed policy catalog + cancel-aware drain are all OSS. 11 tests (every policy: fail within/over bound, drop-oldest tail window, pause-upstream truncation + within-bound, degrade-quality identity; cancel stops the drain; a decode error aborts; empty result; `stream_metadata`; defaults). 2011 axon-rs lib + dispatcher integration suites green, zero regressions. Live cursor execution proven in 35.l. D10. |
| **35.j** | **Pillar IV — Capability-typed store access** | ~750 | ✅ SHIPPED | New `capability: "<slug>"` field on `axonstore` (`axon-frontend` ast/parser/ir_nodes/ir_generator) — slug-validated at parse against the closed grammar shared with Fase 32.g `requires:` (open-question #4 resolved to a dedicated field). **Compile-time enforcement** — the `axon-frontend` type-checker's `check_axonendpoint` resolves the executed flow, walks its top-level store ops, and requires the endpoint's `requires:` to GRANT every capability-gated store's `capability`; a program where an under-privileged endpoint could reach a gated store does not type-check. Data isolation becomes a static language guarantee. (Nested store ops inside `for`/`par` bodies are caught by the runtime re-check.) **Runtime re-check** — new pure `axon-rs/src/store/capability.rs` (`check_store_capability` + `CapabilityDenied`); `DispatchCtx.held_capabilities: Option<Vec<String>>` field + `with_held_capabilities` builder; the 4 dispatcher store handlers gate every access (`enforce_store_capability`, before any side effect) — `Some(held)` enforces (deny → `DispatchError::BackendError`, never a silent read of isolated data), `None` defers to the compile-time guarantee. `streaming_via_dispatcher` threads the request's held capabilities; `server_execute_streaming` + `execute_sse_handler_inner` source them from the JWT bearer via Fase 32.g `extract_capabilities_from_bearer`. **SPLIT** (§6): OSS = the capability-typing mechanism + type-checker enforcement (shipped here); ENTERPRISE = multitenant store-isolation operations (per-tenant capability provisioning, tenant-scoped routing). 15 tests (6 `capability.rs` pure-policy, 5 dispatcher wiring — denied/allowed/ungated/no-context-skip, 4 type-checker — endpoint-must-grant / granted-clean / ungated-clean / malformed-slug-rejected). 1981 axon-rs + 250 axon-frontend lib + 130+ integration tests green, zero regressions. D11. |
| **35.k** | D13 fuzz | ~480 | ✅ SHIPPED | New `axon-rs/tests/fase35_fuzz.rs` — hand-rolled 64-bit LCG (deterministic, seed-reproducible; the Fase 32.i/33.g pattern), 6 surfaces, ~24 000 iterations, every Fase 35 pure public surface total + never-panic. **§1 filter compiler** — arbitrary `where`-expr strings (garbage + structured + injection payloads) → `parse_filter`/`build_pg_where` never panic; the D4 invariant fuzzed: a compiled clause NEVER contains a raw `'`, `;` or `--` (every value is a `$N` placeholder); placeholder count == params count, params never carry `Null`, placeholders sequential from `offset+1`. **§2 closed-catalog** — `classify_backend`/`classify_pg_type`/`resolve_on_breach`/`is_safe_identifier`/`BackpressurePolicy::from_slug`/`resolve_dsn` total over arbitrary strings. **§3 epistemic** — `confidence_of_json`/`confidence_of_sql` over arbitrary values (incl. `f64::from_bits` NaN/Inf), `enforce_retrieve_floor` partition is lossless (trusted + below_floor == input), `enforce_persist_floor` total. **§4 audit-chain** — under an arbitrary delta sequence the HMAC-Merkle chain ALWAYS verifies `Intact`, the head advances monotonically (every head distinct), and the head is HMAC-key-bound (same sequence + different key → different head). **§5 store-resolution** — `StoreRegistry::build` total over arbitrary spec sets (incl. name collisions + `sqlite`/`mysql`/garbage backends), `check_store_capability` total. **§6 SQL builders** — `build_select/insert/update/delete_sql` total + the same injection-resistance invariant as §1. D13. |
| **35.l** | Real-Postgres integration tests | ~600 | ✅ SHIPPED | New `axon-rs/tests/fase35_l_postgres_integration.rs` (12 tests) — every Fase 35 SQL surface exercised **end-to-end against a REAL `postgres:16`**, not mocks. **The harness**: the tests connect via `AXON_TEST_DATABASE_URL`; unset / unreachable → each test **skips gracefully** (logs + returns) so `cargo test` on a Postgres-less developer machine still passes; new `.github/workflows/fase_35_axonstore.yml` provides a `postgres:16` SERVICE container + the env var, so CI runs them for real on every push (3 lanes: no-DB store-runtime + D13 fuzz / real-Postgres D14 / frontend Pillar-IV type-check). Tests: T1-T3 substrate CRUD round-trips (real insert/query/mutate/purge + row counts); T4 Pillar I — `confidence_floor` 0.8 over 5 real `_confidence`-column rows filters the 2 sub-floor; T5-T8 Pillar III — `DropOldest`/`PauseUpstream`/`Fail`/cancel over a real 50-row cursor with `max_rows=10` (real backpressure bounds); T9 type mapping — UUID/TIMESTAMPTZ/NUMERIC/JSONB/BYTEA/INT/FLOAT inserted via raw SQL → `query` → every column JSON-safe (the kivi-monkey-patch pre-emption, proven); T10 Pillar II — three real mutations chained → `verify()` Intact; T11 Pillar IV — a capability-gated postgresql store resolves to a real backend + the gate denies/admits; T12 D4 — a `'; DROP TABLE …; --'` payload in a `where` value is parameterized and inert, the real table survives. **Verified locally against a live `postgres:16` Docker container — all 12 green** (the real-DB run caught a genuine test bug — `"TRUE"` as a `where_expr` parses as a column, match-all is the empty `where` — proving D14's value over the skip path). D14. |
| **35.m** | Adopter docs + `mutate`/`purge` parser-gap fix | ~900 | ✅ SHIPPED | New `docs/ADOPTER_AXONSTORE.md` — the 15-section cognitive data plane reference: what changed in v1.30.0, the `axonstore` declaration (every field), the four store ops, the `where` grammar, all four pillars, the `in_memory` backend, connecting to Postgres, the D12 honest scope boundaries, a production checklist + troubleshooting. New `docs/MIGRATION_v1.30.md` — 5 scenario-driven sections (postgresql store now honored / SQL retrieve is an epistemic envelope / `mutate`+`purge` honor `where:` / capability-gated stores need `requires:` / in-memory unchanged) + the backwards-compat-absolute statement. **Closed a real parser gap discovered while writing the docs**: `mutate`/`purge` parsed via `parse_flow_step_simple`, which *skipped* the braced block — a written `{ where: "<expr>" }` clause was silently dropped, leaving the entire 35.b/c parameterized-filter machinery unreachable for those two ops (every `mutate`/`purge` ran whole-store). New `parse_store_where_step` (mirrors `parse_retrieve_step`, minus the `as:` alias) captures the clause into `MutateStep`/`PurgeStep.where_expr`; the IR generator already propagated it. A half-finished feature made whole — the founder's no-shortcuts bar. 3 frontend parser tests (`mutate`/`purge` capture `where`, no-block → whole-store). Docs are 100% adopter-agnostic. 253 axon-frontend + 2011 axon-rs lib tests green, zero regressions. **SPLIT** (§6): OSS docs shipped here; the enterprise `INTEGRATION_GUIDE.md` hardening-layer extension ships on the enterprise track. |
| **35.n** | Release v1.30.0 cross-stack + axon-enterprise catch-up | release | ✅ SHIPPED | **axon-lang v1.30.0 live cross-stack.** `bump-my-version bump minor` 1.29.1 → 1.30.0 across 5 files (`pyproject.toml`, `axon/__init__.py`, `tests/test_cli_mvp_smoke.py`, `tests/test_frontend_contract_golden.py`, `axon-rs/Cargo.toml`); `axon-frontend` 0.12.0 → 0.13.0 + the `=0.13.0` dep pin. **crates.io**: `axon-frontend` 0.13.0 + `axon-lang` 1.30.0 published. **GitHub Release** `v1.30.0` published with content-first notes (the auto-created `github-actions[bot]` release was edited in place — `gh release create` 422'd on the pre-existing tag). **PyPI**: `publish.yml` did not fire on `release: published` (the known event-drop quirk) — recovered via the draft-toggle (run 25954041511, success, 2m55s). **`rust_release.yml`** (`rust-v1.30.0`) green end-to-end — all multi-arch binary tarballs built (run 25953761688, 21m39s). Incident resolved mid-release: a 100%-full disk (bloated `target/`) was reclaimed via `cargo clean` (486 GiB freed). **axon-enterprise v1.21.0 catch-up live**: PR #26 merged commit `2a3ab27`; version 1.20.2 → 1.21.0 (`pyproject.toml` + `axon_enterprise/__init__.py`) + `axon-lang>=` pin 1.29.1 → 1.30.0 + a v1.21.0 description-changelog entry; tag `v1.21.0` pushed; GitHub Release published with the four-pillar vertical-inheritance notes (HIPAA / FRE 502 legal / BSA-OFAC fintech / FedRAMP government). The PR also fixed a real CI breakage — a nightly-toolchain roll promoted the `-Zsanitizer` ABI mismatch to a hard error, so the TSan doctest pass aborted; mirrored `RUSTFLAGS` into `RUSTDOCFLAGS` so the doctest crate is compiled with the same sanitizer ABI (the `thread` lane is `continue-on-error`, so it never blocked the gate — but the fix makes the lane genuinely green). Enterprise CI 25/25 green on the re-run. **Fase 35 cycle CLOSED — 14 sub-fases 35.a–35.n shipped, D1–D14 ratificadas.** |
| **35.o** | `persist` field-block — parser-gap closure + runtime scoping | ~190 | ✅ SHIPPED | **Post-v1.30.0 adopter-gap follow-up** (kivi-enterprise gap report 2026-05-16) — the SAME class of gap 35.m closed for `mutate`/`purge`, now for `persist`, the third store-op. Pre-35.o `persist` parsed via `parse_flow_step_simple`, which *skipped* the braced block — a written `persist <store> { col: value }` field block was silently dropped, so the runtime fell back to writing every context binding as the SQL row. Any real flow carries more bindings (flow params + step results + retrieve aliases + `let`s) than a table has columns → `INSERT` fails with `column "X" does not exist`; `persist` was unusable against any real table. **Fix**: new `parse_persist_step` (mirror of `parse_retrieve_step`, arbitrary column keys) captures the block into `PersistStep.fields` + `IRPersistStep.fields`; the optional `into` connector is now skipped (pre-35.o `persist into X` captured `into` AS the store name — lateral bug closed). **Runtime, both paths (D5)**: `ExecContext::interpolate`'s core extracted to a free `interpolate_vars(text, &vars)` so the sync runner (`execute_sql_store_step` via new `CompiledStep.persist_fields`) and the streaming dispatcher (`run_persist` via new `persist_row`) build the SQL row from EXACTLY the declared columns, value expressions `${name}`-interpolated, byte-identical. A `persist` with no block keeps the v1.30.0 user-bindings fallback — backward-compatible absolute. The `in_memory` KV path is unchanged (no schema, no failure mode — `fields` is a relational-row concept). 8 new tests (5 frontend parser/IR + 1 runner + 2 dispatcher `persist_row`); 258 axon-frontend + 2014 axon-rs lib tests green, zero regressions. Ships as **axon-lang v1.31.0** + axon-enterprise v1.22.0 catch-up. |
| **35.p** | `mutate` SET-field-block — the symmetric gap closure | ~210 | ✅ SHIPPED | **Post-v1.31.0 follow-up** (founder caught the symmetric gap 2026-05-16) — `mutate` had the *identical* defect 35.o closed for `persist`. `mutate` parsed via `parse_store_where_step`, which captured only `where:` and skipped every other block key, so the runtime built the `UPDATE … SET` clause from every context binding → `UPDATE` failed against any real table (`column "X" does not exist`); `mutate` was unusable. The original gap report mildly misled (*"mutate SÍ honra su bloque"* — true only for `where:`, false for SET-scoping). **Fix**: new `parse_mutate_step` (the block carries `where:` + arbitrary `col: value` SET assignments; `where:` keeps its string-literal grammar; `parse_store_where_step` becomes `purge`-only — a `DELETE` has no SET); `MutateStep`/`IRMutateStep` gain `fields`. **Runtime, both paths (D5)**: 35.o's `persist_row` generalised to `store_row(fields, ctx)` (node-agnostic) — `run_mutate` + the sync runner's `execute_sql_store_step` build the `SET` from exactly the declared columns, `${name}`-interpolated; `CompiledStep.persist_fields` → `store_fields`, materialised for `IRFlowNode::Persist` **and** `Mutate`. No SET column ⇒ v1.31.0 user-bindings fallback (backward-compatible). `in_memory` KV path unchanged. 9 new tests (4 frontend parser/IR + 1 runner + 1 dispatcher `store_row`-for-mutate + the generalised `store_row`/`interpolate_vars` infra reused). Full suites green, zero regressions. New `docs/MIGRATION_v1.32.md` + `ADOPTER_AXONSTORE.md` §4.2. Ships as **axon-lang v1.32.0** + axon-enterprise v1.23.0 catch-up. Inventory closed: `retrieve`/`persist`/`mutate` all scope their columns; `purge` is a `DELETE` (no SET). |

**Total target: ~10 000 LOC + the real-Postgres integration harness + D12 fuzz. Built Rust-canonical (the strategic direction — 0 Python). D3 zero-regression absolute.**

## ▶ 6. OSS / ENTERPRISE / SPLIT classification

The cognitive data plane is **OSS — non-negotiably**. The four pillars are the `axonstore` joining systems axon-lang OSS already ships (the ESK epistemic lattice, the audit-chain base, the `unified_stream_handler`, the capability/trust types). An OSS `axonstore` that could not join them would be a language contradicting its own type — `IRAxonStore` declares `confidence_floor` / `on_breach`, so OSS axon must honor them. Walling the pillars behind enterprise would amputate the language and forfeit the strategic goal (the *default* language for AI software is the OSS one).

**The moat is real — but it lives at the HARDENING layer, not the primitive layer.** This follows the established charter pattern: Fase 20 shipped the Shield *mechanism* OSS and the *vertical scanners* (HIPAA/legal/fintech) enterprise; Fase 27 shipped the C kernels OSS and the *FIPS-validated link* enterprise. The cognitive data plane follows the same line: the **primitive is OSS**, the **regulatory-grade hardening of the primitive is enterprise**.

### Per-sub-fase classification

| Sub-phase | Class | Rationale |
|---|---|---|
| 35.a–35.f (substrate: anchor, filter, backend, registry, both wirings) | **OSS** | The data plane mechanism — adopter-agnostic. |
| 35.g — Pillar I, Epistemic | **SPLIT** | OSS: the epistemic-grade mechanism (rows born `Untrusted`, `confidence_floor` enforcement). ENTERPRISE: per-vertical confidence calibration (what "believe" means for a HIPAA clinical row / a legal-privilege assessment / a fintech AML risk score). |
| 35.h — Pillar II, Audit-chain | **SPLIT** | OSS: the HMAC-Merkle chain mechanism with `sha2`/`hmac` crypto + `on_breach`. ENTERPRISE: the FIPS-validated crypto link + the mmap tamper-evident kernel + the court-admissible evidence packager. |
| 35.i — Pillar III, `Stream<Row>` | **OSS** | The streaming surface is `unified_stream_handler` + the closed `BackpressurePolicy` catalog — all OSS. |
| 35.j — Pillar IV, Capability-typed access | **SPLIT** | OSS: the capability-typing mechanism + type-checker enforcement. ENTERPRISE: the multitenant store-isolation operations (per-tenant capability provisioning, tenant-scoped connection routing, per-tenant chain segregation). |
| 35.k–35.l (fuzz, real-Postgres integration) | **OSS** | The robustness gates for the OSS primitive. |
| 35.m (docs) | **SPLIT** | OSS: `docs/ADOPTER_AXONSTORE.md`. ENTERPRISE: the `INTEGRATION_GUIDE.md` extension for the hardening layer. |
| 35.n (release) | **SPLIT** | axon-lang v1.30.0 (OSS) + axon-enterprise catch-up. |

### The axonstore Regulatory Hardening Layer (enterprise track)

The enterprise product is a coherent, named surface — the **axonstore Regulatory Hardening Layer** — that layers on top of the OSS cognitive data plane. It does NOT gate the primitive; it makes the primitive *audit-defensible before a regulator*. Six components:

1. **FIPS-validated audit-chain crypto.** Pillar II's HMAC-Merkle chain re-routed through the `axon-csys-enterprise` BoringSSL / OpenSSL-FIPS link (FIPS 140-3 posture for HIPAA / SOC 2 / FedRAMP). Wire-byte-identical to the OSS pure-C path → cross-deployment verification.
2. **mmap tamper-evident append-only kernel.** The chain's storage backed by the `axon-csys-enterprise` mmap kernel (~290k events/sec, per-tenant HMAC chain + segment rotation) instead of a plain Postgres sidecar.
3. **Court-admissible evidence packager.** Export a store's mutation chain as a byte-deterministic evidence bundle (ZIP STORE + canonical-JSON manifest + Merkle root + Ed25519 signature) — the Fase 27 enterprise evidence packager applied to `axonstore`.
4. **Vertical confidence policies (Pillar I R&D).** Per-vertical calibration of `confidence_floor` semantics — HIPAA clinical-data confidence, legal privilege confidence, fintech AML risk-score confidence, government decision-support confidence. OSS ships the *mechanism*; enterprise ships the *regulator-defensible calibration*.
5. **Multitenant store-isolation operations (Pillar IV ops).** Per-tenant capability provisioning, tenant-scoped connection routing (the `axon/tenants/*` Secrets Manager namespace), per-tenant audit-chain segregation, tenant-isolation invariant gates — the operational multitenancy the enterprise charter owns.
6. **Per-vertical regulatory replay + retention.** `GET /v1/replay` extended to the store mutation chain; per-vertical retention policies (PCI DSS Req 10 / FedRAMP AU-2 / 21 CFR Part 11 §11.10 / FRE 502).

**Sequencing:** v1.30.0 ships the OSS cognitive data plane (35.a–n). The Regulatory Hardening Layer ships as a subsequent **axon-enterprise** release on the enterprise vertical R&D track — the same shape as Fase 20's enterprise Shield layer landing after the OSS mechanism. The SPLIT sub-fases (35.g/h/j) design their OSS surface with the documented enterprise override seam from day 1, so the hardening layer slots in without an OSS refactor.

## ▶ 7. Open scoping questions for the ratification bloque

1. **Pillar sequencing.** All four pillars (35.g–j) in v1.30.0 — or substrate + a subset in v1.30.0, the rest in v1.30.x? Recommendation: all four — the substrate alone is the "industry-standard" version the founder explicitly rejected; the pillars ARE Fase 35.
2. **`confidence_floor` storage (Pillar I).** ✅ **RESOLVED in 35.g** — the reserved `_confidence` column convention (a sidecar table would need `CREATE TABLE` DDL, which D12 forbids in v1.30.0). `axon-rs/src/store/epistemic.rs::CONFIDENCE_COLUMN`. An adopter declaring `confidence_floor` carries a `_confidence` column on their table; a row with no `_confidence` is at ⊥.
3. **Audit-chain storage (Pillar II).** ✅ **RESOLVED in 35.h** — the OSS chain is **in-process, flow-lifetime** (`DispatchCtx`'s `StoreAuditChain` — no sidecar table, so no `CREATE TABLE` DDL, honoring D12). Cross-process persistent storage is the enterprise mmap tamper-evident kernel (§6). `on_breach` is the closed catalog **`{log, raise, rollback}`** — the existing `VALID_STORE_ON_BREACH` the type-checker already enforces (NOT `{halt, quarantine, alert}` — that would have diverged from the ratified catalog).
4. **Capability grammar (Pillar IV).** ✅ **RESOLVED in 35.j** — a dedicated `axonstore S { capability: "<slug>" }` field (one capability gates one store; a `requires: [list]` would imply unclear AND-semantics). The slug grammar itself IS reused from Fase 32.g (`is_valid_capability_slug`, the closed `^[a-z][a-z0-9_]*(\.[a-z][a-z0-9_]*)*$`), validated at parse time.
5. **`sqlite` backend.** Python ships `sqlite_backend.py`. Include a Rust `sqlite` backend in the closed catalog for v1.30.0, or keep `{in_memory, postgresql}` and add sqlite later?

---

*This plan vivo is the Fase 35 source of truth. Built Rust-canonical per the 0-Python strategic direction. Sub-fase status flips ⏳ → ✅ SHIPPED at landing. D-letter text is frozen on founder bloque ratification.*
