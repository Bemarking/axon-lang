---
title: "Plan vivo: Fase 41 — WebSocket as a Cognitive Primitive (session-typed bidirectional dialogue)"
status: 📝 PROPOSED — research grounded (see docs/paper_websocket_cognitive_primitive.md); awaiting founder "procede con 41.a". The bidirectional dual of Fase 33 (SSE as a cognitive primitive). Elevates RFC 6455 from an untyped 3-frame channel to a session-typed dialogue: the connection carries a session type (a linear-logic proposition), is well-formed iff endpoints are dual ($\bar S = S^{\perp}$) → deadlock-free + protocol-conformant by construction, with backpressure as a credit-refined index decidable in Presburger arithmetic.
owner: AXON Compiler + Runtime + Enterprise Team
created: 2026-05-22
target: |
  axon-lang **v2.3.0** — MINOR (additive new language primitive: the `socket`
  block + session-type checker in axon-frontend + the typed-WS runtime in
  axon-rs over the Fase 13 π-channels). Backwards-compatible.
  axon-enterprise **catch-up** — the multi-tenant, audited WebSocket SURFACE on
  the enterprise server (axum/tokio WS endpoint + RLS + audit chain + shield),
  which is what lets the demanding adopter (Kivi) collapse to a SINGLE image.
depends_on: |
  Fase 13 SHIPPED (Mobile Typed Channels — π-calculus typed channels + capability
  extrusion; the realization substrate for endpoints).
  Fase 33 SHIPPED (SSE as a cognitive primitive — the single-polarity fragment
  this generalizes; S_SSE = Π↓(S_WS)).
  Fase 40 CLOSED (axon-enterprise 100% Rust/C, v2.0.1; cognitive_states sealed
  snapshots for reconnection; the enterprise axum server + audit chain + RLS).
charter_class: |
  LANGUAGE PRIMITIVE (axon-for-axon). The primitive is OSS axon-lang (the type
  discipline + runtime); the enterprise exposes it multi-tenant/audited. Driven
  by a demanding production adopter (a large SaaS agent: skills, multiple chat
  types, websocket, webhooks, multi-tool, microservices) whose real need defines
  the primitive — the adopter makes the language better.
---

## Why Fase 41 exists

A demanding Enterprise adopter (a large multi-skill SaaS agent) wants to collapse
its deployment to a **single image** (the axon-enterprise binary, which already
runs flows + 7 LLM backends + SSE + dynamic REST + the control plane). The one
surface the Rust enterprise server does **not** yet expose is **WebSocket** (the
old Python image bound port 8001 for it; the v2.0.x image exposes only 8080 HTTP).
Rather than bolt on a raw socket, Fase 41 does the axon thing: **re-found
WebSocket as a cognitive primitive**, grounded on a top-tier paper across the four
pillars (MATHEMATICS · LOGIC · PHILOSOPHY · COMPUTATION). See
[docs/paper_websocket_cognitive_primitive.md](../paper_websocket_cognitive_primitive.md).

The gap it closes (measured): RFC 6455 frames only `text`/`binary`/`control` and
delegates protocol meaning to the application; the industry types **messages**
(Zod/tRPC/JSON-Schema), never the **conversation**; and the reference stacks
concede the interface "does not support backpressure." Fase 41 types the
conversation and makes backpressure a decidable type-level invariant.

## The two-question gate (founder directive)

1. **Market standard or superior?** **Superior.** State of the art types individual
   messages (tRPC/Zod discriminated unions) and handles backpressure operationally
   (manual pause/resume). Fase 41 types the **protocol-as-session** (duality-checked
   ⇒ deadlock-free + conformant by construction, after Caires–Pfenning /
   Honda–Yoshida–Carbone) and makes **backpressure a credit-refined linear index**
   (decidable in Presburger, after Rast). No deployed WebSocket stack does this.
2. **Minimum to run, or robust for large adopters?** **Robust.** Multi-tenant RLS
   isolation + per-utterance audit-chain provenance + shield/legal-basis on every
   move + typed reconnection via sealed `cognitive_states` + multiparty projection
   for n-agent (skills/tools) topologies. Deferred (enumerated): horizontal
   fan-out across nodes (Redis/relay) and the browser-side client codegen.

## Decisions

- **D1** — **Session types are the discipline** (not per-message schemas). A
  `socket`'s `protocol` IS a session type; the connection law is $\bar S = S^{\perp}$
  (§3.2 of the paper). Grounded in the linear-logic Curry–Howard (Caires–Pfenning).
- **D2** — **Backpressure is a typed resource**: the credit-refined send ${!}^{n}A.S$;
  a send at $n=0$ has no typing rule (compile error, not a memory blow-up). Side
  conditions discharged in Presburger arithmetic → terminating compiler pass.
- **D3** — **SSE is the single-polarity fragment, not a separate thing**:
  $S_{\mathrm{SSE}} = \Pi_{\downarrow}(S_{\mathrm{WS}})$. Generalize Fase 33; do NOT
  fork the stream theory. One stream/dialogue theory.
- **D4** — **Realize endpoints over the Fase 13 π-typed channels** + Rust `tokio`
  WS. The OSS runtime owns the transport + typing; the enterprise owns the
  multi-tenant/audited surface (charter split).
- **D5** — **Reconnection via `cognitive_states`** (Fase 40.t): on disconnect, seal
  the session *continuation* (the residual session type + state) as an
  envelope-encrypted snapshot; a typed `reconnect` resumes only at a continuation
  the session type admits.
- **D6** — **Multiparty (n>2) via global-type projection** (Honda–Yoshida–Carbone)
  for many-skilled agents: one declared global dialogue $G$, endpoints =
  $\{G\!\restriction\! r\}$. (Sub-fase 41.h; binary first.)
- **D7** — **shield + audit on every utterance** (enterprise): each move is
  capability/PII-mediated by `shield` and anchored in the per-tenant SHA-256 audit
  chain — a typed, auditable dialogue.
- **D8** — **Absolute backwards-compat**: additive primitive; existing flows + the
  SSE primitive unchanged; wire byte-compat preserved (a `socket` with a
  single-polarity protocol negotiates an SSE-equivalent stream).

## Sub-fases (topologically ordered; each gated by an explicit founder "procede")

| Sub-fase | Scope |
|---|---|
| **41.a** ✅ SHIPPED 2026-05-22 | **Session-type algebra + duality** in `axon-frontend` (pure): the type grammar (§3.1), the duality involution $(\cdot)^{\perp}$ + the regular-coinductive equality for $\mu$-types, the binary connection-law check $\bar S \stackrel{?}{=} S^{\perp}$. Property-tested (involutivity, duality round-trip). | ✅ New `axon-frontend/src/session.rs` (pure, no runtime deps): `SessionType` grammar (`end`/`!A.S`/`?A.S`/`⊕`/`&`/`μX.S`/`X`) + smart constructors; `dual()` involution (swaps send↔recv, select↔branch; preserves payload + recursion); capture-stopping `subst` + equirecursive `unfold_head`; **`equiv()`** = the regular-coinductive equality (assume-pair-equal → unfold leading Recs → compare heads → recurse; terminates on regular types); **`is_dual_to()`** = the connection law (`peer ≡ self⊥`). Payload opaque (canonical type name; 41.b binds real AST types — the algebra depends only on payload equality). **10 tests green** (duality swaps + payload preservation, involutivity incl. recursive, connection law accept-dual/reject-self+mismatch, equirecursive fold/unfold + α-equivalence, equality negatives, a realistic recursive chat dialogue that terminates without blow-up). Zero new warnings. Commit `1c2149f`. (Multiparty projection moved to 41.h per D6 — 41.a is the binary algebra.) |
| **41.b** ✅ SHIPPED 2026-05-22 | **The `socket` primitive surface**: parser + AST + IR for the `socket { protocol, backpressure, duality, reconnect, legal_basis }` block; the session-type concrete syntax (`send`/`recv`/`select`/`branch`/`rec`/`end`). Cross-stack drift gate (Rust frontend ⇄ Python wrapper). | ✅ **Three landed pieces** (all in `axon-frontend`): **(1) Duality rewired** (commit `653c4a3`) — the Fase 4 `session` surface (`SessionDefinition`/`SessionRole`/`SessionStep` = `send`/`receive`/`loop`/`end`) was already in the language; new `lower_session_role` maps a role step-list into a §41.a `SessionType` (`loop` ↦ `μX.…X`), and `check_session_duality` is rewired from the old ad-hoc positional check (equal-length + pairwise `steps_dual`, which treated `loop` as a token, not recursion) to the rigorous **connection law** `T₂ ≡ T₁⊥` via regular-coinductive `is_dual_to` — recursion-correct, linear-logic-grounded. Retired `steps_dual`/`format_step`. **(2) `socket` declaration** (commit `d8a7d27`, surface A chosen by founder — protocol + transport as separate-but-composable concepts): `socket` keyword (lexer + declaration keyword) + `SocketDefinition` AST (`protocol`/`backpressure_credit`/`reconnect`/`legal_basis`) + `parse_socket` (order-free `key: value`; `backpressure: credit(n)`, `reconnect: cognitive_state`, `legal_basis`) + `IRSocket` (+ `IRProgram.sockets` + `visit_socket`) + `check_socket` (protocol must reference a **declared `session`**; backpressure credit must be **≥ 1** — a 0-credit window can't type a send, §4.2). **(3) Choice grammar** (commit `cfd5ab5`) — extends `SessionStep` with `branches: Vec<SessionBranch>` (each `{ label, steps }`) and `select`/`branch` ops; `parse_session_choice` parses `select { ℓ: [steps], … }` (and `branch { … }`); `lower_session_steps` maps `select→⊕`, `branch→&` over the algebra, duality decided arm-for-arm by the connection law (`select ⊥ branch` via `dual_map`); `check_session_steps` recurses into arms, requires ≥1 branch with **unique labels**; `IRSessionStep.branches` + `IRSessionBranch` lowered recursively by `lower_session_step_ir`. **14 Fase 41.b tests** (4 lowering + 4 socket + 6 choice: parse, lower, `select`⊥`branch`, clean typecheck, duplicate-label reject, empty-choice reject); **491 lib tests green**, zero regressions, zero new warnings. Sufficient session-type expressiveness for 41.c (credit-refined `!ⁿ/?ⁿ`) and 41.d (runtime over π-channels). |
| **41.c** ✅ SHIPPED 2026-05-22 | **Credit-refined backpressure typing** (D2): the index ${!}^{n}/{?}^{n}$, the `credit(k)` annotation, the Presburger discharge of credit constraints; the "no rule at $n=0$" error. Decidability + termination tests. | ✅ **Algebra extended + Presburger discharge wired** (commit `2239eaf`). `SessionType::Send`/`Recv` refactored to struct variants carrying `credit: Option<u64>` — `None` is the unbounded fragment (the pre-41.c algebra, preserved by default), `Some(n)` is `!ⁿA.S` / `?ⁿA.S` (paper §4.2). Smart constructors split: `send`/`recv` stay unbounded (backwards-compat across all 41.a/41.b call sites); new `send_credit`/`recv_credit` for the refined fragment. **Duality preserves credit symmetrically** (`(!ⁿA.S)⊥ = ?ⁿA.S⊥` — sender's window-of-n = receiver's absorbing capacity; standard credit-flow semantics, Rast lineage). Equivalence distinguishes credit indices ⇒ structurally distinct types. Display: `!^n A.S` / `?^n A.S`. New algebra methods: `with_credit(n)` (idempotent stamp), `has_send_at_zero()` (the explicit `!⁰A.S` axiom — unprovable by construction, returns offending payload in linear time), `recurring_paths(x)` (every path from root to `Var(x)` — terminating arms like `cancel:end` exempt from sustainability), `credit_delta(x)` (worst-case per-iter (#send, #recv)). **`credit_analyse(budget) → Result<(), CreditError>`** is the Presburger discharge proper: abstract-interprets the type starting at full window, tracking the available-credit fixpoint across straight-line (send: avail−1; recv: min(avail+1, k)), choice (worst-arm conservative), and recursion (every recurring path must satisfy `Δ = #send − #recv ≤ 0` — the loop-fixpoint inequality; non-recurring arms exempt). Three verdict witnesses: `SendAtZero {payload}`, `BurstOverflow {payload, budget, burst}`, `LoopUnsustainable {sends_per_iter, recvs_per_iter}`. **Wired into `check_socket`**: after 41.b's protocol + credit≥1 gates, each role is lowered, stamped with `k`, and run through `credit_analyse(k)`; verdicts emitted as `socket '…' violates the credit-refined backpressure type of session '…' role '…': <CreditError> (D2)`. **Unbounded fragment** (no `backpressure:` annotation) skips analysis cleanly — pre-41.c default preserved. **+19 tests**: 12 algebra (dual preserves credit, equivalence distinguishes, with_credit idempotent, has_send_at_zero, credit_analyse accepts within-budget straight-line + rejects burst overflow + rejects explicit n=0 + rejects unsustainable loop Δ=2-1>0 + accepts balanced loop Δ=0 + walks choice arms worst-case + credit_delta on recurring paths only + total on realistic recursive chat dialogue with no stack blow-up) + 7 integration (`fase41c_credit_tests`: within-budget accepted, burst-overflow rejected on producer-only with server clean, unsustainable loop rejected at any budget incl. credit(100), balanced loop accepted at minimal budget=1, choice arms each checked, no-annotation skips analysis, zero-credit still caught by 41.b separately). **510 lib tests green**, zero regressions, zero new warnings. Decidability + termination: constraints are linear over the naturals (Presburger-decidable); the algorithm is direct abstract interpretation in linear time over type size × #choice leaves. |
| **41.d** ✅ SHIPPED 2026-05-22 | **Runtime: typed WS endpoint over π-channels** in `axon-rs`: a `tokio` WebSocket realized as a Fase 13 typed channel; the send/recv/select/branch operational rules; credit accounting at runtime. E2E against a local axum WS mock. | ✅ **New `axon-rs/src/session_runtime/` module** (commit `d577aec`, ~800 LOC across 4 files + tests). **Carrier-agnostic core + RFC 6455 binding**: `SessionRuntime` is the operational state machine — cursor (always head-unfolded via `SessionType::unfold_head`, promoted to `pub`) + `CreditWindow` (TCP-window semantics: consume on send, refill on recv capped at budget) + one method per algebra rule (`try_send`/`try_recv`/`try_select`/`try_offer`/`try_end`). `ProtocolError` is a closed catalog of runtime witnesses — `PayloadMismatch`, `UnexpectedFrame`, `UnknownLabel`, `CreditExhausted` (the §41.c "no rule at n=0" axiom at runtime), `AlreadyComplete`, `MalformedFrame`, `Transport` — each with a stable `code()` ≤ 123 bytes UTF-8 for the RFC 6455 close-frame reason payload. Wire format = closed-catalog JSON envelope `{v:1, kind, …}` — `Send {payload_type, data}`, `Select {label}`, `End`, `Error {code, detail}`; version-first key ordering via deterministic string splice (default `serde_json::Map` is BTreeMap-backed → alphabetical, which would land "kind" before "v"; splice avoids `preserve_order` feature). `ws::drive(WebSocket, SessionRuntime, PeerRole) → Result<(), ProtocolError>` is the protocol loop: when cursor is producer-state, emit outgoing frame + step local runtime first; else read peer frame, parse + apply via `try_recv`/`try_offer`/`try_end`; on `End` (both sides) close `1000 normal closure`; on `ProtocolError` (either side, including our own credit-exhaustion) emit `Frame::Error` then close `1002 protocol error` with the error's `code()` as reason. Cargo.toml: enable `axum` `ws` feature; add `tokio-tungstenite = 0.24` dev-dep (same library axum pulls server-side ⇒ one common WS impl across both peers). **+26 unit tests** (11 state + 12 wire + 3 ws) + **4 E2E** scenarios against a real axum server bound on `TcpListener::bind("127.0.0.1:0")` with a `tokio-tungstenite::connect_async` client (true bytes-on-wire, not `tower::oneshot`): happy-path runs to completion + closes 1000; payload mismatch → Error(payload_mismatch) + close 1002; unexpected frame kind → Error(unexpected_frame) + close 1002; credit exhaustion at runtime (budget=1 on `!A.!B.end`, server's second send hits n=0) → Error(credit_exhausted) + close 1002. **Bug caught in vivo by scenario 4**: first draft used `?` on apply_outgoing errors, silently closing the carrier without the Error frame. Fix: explicit match → `report_and_close` before returning `Err`. **Carry-fix from 41.b**: 3 manual `IRProgram` constructions in `cost_estimator.rs` tests didn't have the new `sockets: Vec<IRSocket>` field; added `sockets: vec![]` (the 41.b ship verified only `cargo build --lib`, not lib-test, so this surfaced now). **2231 axon-rs lib tests green** (+26 new), **510 axon-frontend lib tests green** (+0 — `unfold_head` pub promotion is a pure surface change), zero regressions across both stacks. |
| **41.e** ✅ SHIPPED 2026-05-22 | **SSE-as-fragment unification** (D3): prove + wire $S_{\mathrm{SSE}} = \Pi_{\downarrow}(S_{\mathrm{WS}})$; a single-polarity `socket` negotiates an SSE-equivalent stream (Fase 33 reuse, byte-compat). | ✅ **Both proven + wired** (commit `a07b1f5`). **Proof (algebra)** — `axon-frontend/src/session.rs`: new `Polarity` enum {Producer, Consumer} (the two SSE-projectable polarities related by duality; `flip()` involutive); new `SessionType::projects_to_sse(&self) -> bool` (linear-time scan: true iff `End / Send / internal-Select / Rec / Var` only — no `Recv`, no `Branch`); dual `projects_to_sse_consumer`; unified `has_polarity(p: Polarity) -> bool`. **+7 tests** proving the §4.4 partition: pure-send chain is producer-fragment; any `Recv` or `Branch` disqualifies; `Select` recurses arm-by-arm (one bad arm fails the choice); the canonical recursive token stream `rec X. !T.X` (Fase 33's shape) is in the fragment; symmetry `S.projects_to_sse() ⇔ S.dual().projects_to_sse_consumer()` over positive + negative samples; polarity flip involutive. **Wire (runtime)** — new `axon-rs/src/session_runtime/sse.rs` (~240 LOC): closed catalog of SSE event names `axon.send` / `axon.select` / `axon.end` / `axon.error` (the §4.4 projection of `Frame::{Send,Select,End,Error}` minus the `v` envelope key — SSE's Content-Type disambiguates). `drive_sse_producer(SessionRuntime) → impl Stream<Item = Result<Event, Infallible>> + Send + 'static` uses `futures::stream::unfold` over a `(runtime, done)` state pair. Preflight short-circuits non-SSE schemas with one `axon.error` event. Per step: cursor at `End` → emit `axon.end` + mark done; `Send`/`Select` → emit + advance via the shared `next_outgoing_frame` / `apply_outgoing` (promoted to `pub(super)` in `ws.rs` — both carriers run the SAME operational dispatch, only the framing differs); runtime `ProtocolError` (canonically credit exhaustion = §41.c n=0 axiom) → one final `axon.error` + close. **+5 sse unit tests**. **Bug caught in vivo**: first draft of `step_runtime` relied on `next_outgoing_frame` for the End case, but that function returns `None` for `End` (WS driver handles End at its outer loop); fix = explicit `is_complete()` check at the top, emits `axon.end` + marks done. **E2E** — new `tests/fase41_e_sse_fragment_e2e.rs`: real axum server on TCP-bound ephemeral port + `reqwest::Client` with `Accept: text/event-stream`; 4 scenarios verifying (1) W3C SSE framing (Content-Type `text/event-stream`, `\n\n` event terminator, sequence `axon.send/axon.send/axon.end` for `!A.!B.end`); (2) session-typed payloads (`axon.send` `data:` line is JSON envelope `{payload_type, data}` — mirrors WS `Frame::Send` minus `v`); (3) polarity preflight (`!Q.?Ack.end` returns exactly one `axon.error` + closes; error envelope `{code, detail}` matches WS `Frame::Error` byte-for-byte — D3 byte-compat across carriers); (4) credit exhaustion at runtime (`!A.!B.end` budget=1 → `axon.send` then `axon.error{code:"credit_exhausted"}` — §41.c axiom on SSE, wire identical to WS scenario modulo framing). **2236 axon-rs lib tests green** (+5), **517 axon-frontend lib tests green** (+7), **4 (41.d) + 4 (41.e) E2E green**, zero regressions cross-stack. |
| **41.f** ✅ SHIPPED 2026-05-22 | **Enterprise WebSocket surface** (the Kivi unblock): the axum/tokio WS endpoint on the enterprise server, gated by the §40.w auth + RLS layer (tenant-scoped by construction), shield + audit per utterance, `EXPOSE` wired. This is what lets the adopter run real-time chat on the single enterprise image. | ✅ **Mounted in axon-enterprise** (commit `5a1a0bc`) + axon-lang `session` re-export companion (`0b62f83`). New module `crates/server/src/ws_session.rs` (~480 LOC) drives the public `axon::session_runtime::SessionRuntime` over an axum `WebSocketUpgrade` carrier under the existing `require_auth` layer; the §Fase 40.c vertical shield runs synchronously per peer `Frame::Send` BEFORE `try_recv` advances the runtime, and every accepted/denied/closed event seals an audit row in the §40.o hash chain via a new `AuditRepo::append` (PgAuditRepo + MockAuditRepo both impl). **Closed-catalog discipline preserved across both axes**: (a) `socket:connect` added to the RBAC catalog (40→41 entries; granted to owner+admin auto + developer explicit; viewer NOT — read-only kept); (b) 4 new `AuditEventType` variants `Session{WsOpened,WsUtterance,WsDenied,WsClosed}` + new `Session` `EventCategory` (catalog 70→74, 12→13 categories; `event_type VARCHAR(64)` no CHECK so no DB migration). **Route registered** `GET /api/v1/socket/{name}` in `build_router` + `route_manifest` (drives `/openapi.json` automatically). `SessionCatalog` / `SessionEntry` form a closed in-process registry of named protocols; bootstrap loads the canonical chat dialogue from the paper `μX. ⊕{ ask: !Utt. &{ token: ?Tok.X, done: end }, cancel: end }` (`SessionCatalog::with_chat_default(Some(8))`) — production extends from `IRSocket` declarations. `ShieldVerdict {Allow{findings}, Reject{code, detail, findings}}` + `ShieldFn = Arc<dyn Fn(&Principal, &str, &str, &Value) → ShieldVerdict + Send + Sync>` (sync — §40.c scanners are deterministic pattern matchers); `permissive_shield()` for tests + the default boot. **Audit-row payload safety**: every `session:ws_utterance` row carries `payload_type` + SHA-256 hex of the data (dogfoods `axon-csys-enterprise::sha256` — same digest the audit chain itself uses) — **never the payload bytes**. Reject codes are stable + machine-readable (`hipaa.phi`, `aml.pan`, `legal.privilege`), safe for client-side error UI without re-revealing content. **Local-dev path-override** (root `Cargo.toml`): `axon-lang = { path = "../axxon-constructor/axon-rs", version = "=2.2.0" }` to consume the in-development Fase 41 APIs — D1 honoured (the `version` constraint declares the target crates.io pin); 41.i coordinated release restores `=2.3.0`. **Companion in axon-lang**: re-export `axon_frontend::session` from `axon-rs/src/lib.rs` so downstream consumers reach the algebra via the single `axon-lang` workspace dep (one extra dep line saved across the org). **+8 ws_session unit tests** (chat schema sustainability — caught a `credit_delta(x)` body-scope bug on the way; catalog round-trips; audit-request builder; permissive shield; session-id uniqueness; canonical select arm pick). **+5 E2E** in `crates/server/tests/fase41_f_ws_session_e2e.rs` against a real axum on `TcpListener::bind("127.0.0.1:0")` + `tokio-tungstenite::connect_async`: (1) **401 unauth** — no Bearer ⇒ auth fail-closed at handshake; (2) **403 missing cap** — `viewer` JWT ⇒ authorize rejects; (3) **404 unknown socket** — empty catalog ⇒ `socket_not_found`; (4) **happy path** — `?Msg.end` schema, client sends Msg, server emits `End` + closes 1000, audit chain `[opened, utterance, closed]` in order, same `resource_id`, utterance row carries 64-char SHA-256 hex; (5) **shield deny** — stub-shield rejects, server emits `Frame::Error{code:"shield_denied:hipaa.phi"}` + closes 1002, audit chain `[opened, DENIED, closed]` (NO ws_utterance — verdict path skip preserved). **Cross-stack regression** all green: axon-frontend lib **517**, axon-rs lib **2236**, axon-enterprise-server lib **86** (+8 new), axon-enterprise-rbac lib **17**, axon-enterprise-audit lib **24**, axon-enterprise WS E2E **5** (new). Zero regressions across both stacks. **`EXPOSE` requirement**: no new port — WS is HTTP upgrade on the existing port 8080 (the canonical pattern); `Dockerfile.enterprise` already exposes it. Kivi single-image acceptance unblocked. |
| **41.g** ✅ SHIPPED 2026-05-22 | **Typed reconnection via `cognitive_states`** (D5): seal the residual session type + state on disconnect; the typed `reconnect` resume; replay/expiry defence (reuse Fase 40.t AAD-bound snapshots). | ✅ **Both sides landed**: axon-lang commit `4608276` (the language-level primitive) + axon-enterprise commit `41323e9` (the §40.t integration). **axon-lang** — `Serialize`/`Deserialize` on `Payload` (`#[serde(transparent)]`), `SessionType`, `CreditWindow`, `SessionRuntime`. New `SealedRuntime { version, schema, cursor, credit }` envelope (stable JSON, `SEALED_RUNTIME_VERSION = 1`). New `SessionRuntime::seal() -> Option<SealedRuntime>` (None at End — completed dialogue has no residual; caller evicts instead of seals); new `SessionRuntime::resume(SealedRuntime, declared_schema) -> Result<SessionRuntime, ResumeError>` validating envelope version + sealed-schema equiv (regular-coinductive — α-equivalent recursion variables accepted). `ResumeError {UnsupportedVersion, SchemaMismatch, Malformed}`. +9 unit tests for the round-trip + every rejection branch. **axon-enterprise** — saas-audit catalog 74 → 77 (`SessionWsSealed` / `SessionWsResumed` / `SessionWsResumeRejected`); new `CognitiveStateRepo` trait (persist / restore / evict) + `MockCognitiveStateRepo` (Mutex<BTreeMap> with introspection) + `PgCognitiveStateRepo` (delegates to `axon_enterprise_persistence::PgCognitiveStore` under tenant advisory lock); `SessionEntry.snapshot_ttl_secs: Option<u64>` (None = non-resumable one-shot; Some = resumable for N seconds — chat default 300s); `ws_socket_handler` takes `Query<WsQuery>`, `?resume=<session_id>` triggers `try_resume` BEFORE upgrade — five typed `ResumeRejection` witnesses (NotFound / Expired / AadMismatch / MalformedEnvelope / SchemaDrift), each → HTTP 410 Gone with stable code; `X-Axon-Session-Id` response header at handshake (the client's resume handle); `run_session_loop` refactored to `SessionLoopCtx` struct — clean End evicts any prior snapshot (D5 replay defence), mid-protocol error + resumable socket → `seal_residual` writes plaintext = `SealedRuntime::to_bytes()` through `cognitive_states::seal_state` (AAD = tenant + session + flow_id + user; §40.k AES-256-GCM + HKDF subkey via `LocalEnvelope`), audit row carries cursor_kind + ciphertext size + expiry (NEVER plaintext). HTTP 410 mapping placed BEFORE the `_not_found` catch-all so `resume_not_found` resolves to 410 not 404 (bug caught in vivo). **Per-loop tenant rebind**: `WebSocketUpgrade::on_upgrade` spawns the loop in a NEW tokio task — the `require_auth` middleware's `scope_tenant` task-local does NOT inherit; without re-scoping, `current_tenant_id()` inside the loop falls back to "default" and the seal's AAD binds the wrong tenant (resume then rejects the legitimate snapshot as `resume_aad_mismatch`). Fix: wrap loop_fut in `scope_tenant(principal.tenant_id, …)`. Bug archived. **4 E2E** in `tests/fase41_g_ws_session_resume_e2e.rs` (real axum + tokio-tungstenite + reqwest for the 410-rejection probe): (1) **happy resume round-trip** — drop after first send → snapshot lands → `?resume=<id>` → complete second send → close 1000; audit chain `opened → utterance → closed(failure) → sealed → resumed → utterance → closed(success)`; snapshot evicted post-clean; (2) **AAD mismatch** — seal under socket alpha, resume under beta → §40.k envelope rejects (flow_id differs) → 410 + `resume_aad_mismatch` + `ws_resume_rejected` audit row; (3) **unknown session** — empty store + garbage id → 410 + `resume_not_found`; (4) **clean-end eviction** — seal → resume-and-complete → second resume against same id → 410 + `resume_not_found` (D5 replay defence — clean end cannot be replayed). **D5 honoured end-to-end**: AAD binds tenant + session + flow_id (the GCM `aad` IS the canonical-AAD passed to HKDF subkey derivation, so cross-binding grafts fail decryption immediately); TTL check fires BEFORE decryption (`open_state` returns `Expired` without ever calling the cipher); clean-end eviction makes resume one-shot-per-disconnect; resume codes are stable + machine-readable for client UI. **Cross-stack regression**: axon-frontend 517 + axon-rs 2245 (+9) + axon-enterprise-server 86 + rbac 17 + audit 24 + 5 (41.f E2E) + 4 (41.g E2E new). Zero regressions cross-stack. |
| **41.h** ✅ SHIPPED 2026-05-22 | **Multiparty projection** (D6): global type $G$ + `G\!\restriction\! r` for n-agent skill/tool topologies; safe-realizability gate. | ✅ **Algebra + gate live** (axon-lang commit `62e549e`). New `axon-frontend/src/multiparty.rs` (~860 LOC incl. tests) ships the Honda–Yoshida–Carbone (POPL'08) multiparty session-type theory. Grammar (paper §5.1): `G ::= end | p→q:T.G | p→q:{ℓᵢ:Gᵢ} | μX.G | X`. Types: `Role(String)` (`#[serde(transparent)]`); `GlobalType` enum with `Serialize`+`Deserialize`+`Hash` (so a deployment can fingerprint a declared protocol before issuing snapshots that bind to it); `ProjectionError {SelfMessage, EmptyChoice, MergeFailed{role, labels, left, right}, UnboundVariable}`. **Projection** (§5.2): `end⌐r=end`; `(p→q:T.G)⌐r=!T.(G⌐r)` if `r=p`, `?T.(G⌐r)` if `r=q`, else `G⌐r` (uninvolved role skips); `(p→q:{ℓᵢ:Gᵢ})⌐r=⊕{ℓᵢ:Gᵢ⌐r}` if `r=p`, `&{ℓᵢ:Gᵢ⌐r}` if `r=q`, else `merge_i(Gᵢ⌐r)` (every arm must project to ≡-equivalent local types for r — else `MergeFailed`); `μX.G⌐r=μX.(G⌐r)` with non-participation elision (role absent from body → `End`) and vacuous-Rec elision (body has no `Var(var)` → unwrap). **Safe-realizability gate** = `GlobalType::project_all() -> Result<BTreeMap<Role, SessionType>, _>` — the `Ok` verdict is the structural certificate that independent per-role runtimes faithfully realise `G`. The merge predicate IS the §41.a regular-coinductive `equiv` (α-equivalent recursion variables accepted). Surface API: `GlobalType::message`/`choice`/`rec`/`var` smart ctors; `roles()` → `BTreeSet<Role>`; `project(role)`; `project_all()`; `is_safely_realizable()`. **axon-rs `lib.rs` re-exports `multiparty`** so consumers reach `Role` + `GlobalType` via the single workspace dep. **+18 unit tests**: sender/receiver/uninvolved projection, internal/external choice from chooser/offerer, merge passes on equivalent arms + fails on divergence, recursion round-trip + the elision optimisation, gate rejections (self-message + empty choice), `roles()` recursive walk, three-role chain projection (User/Agent/Tool, every role's projection pinned to exact equality), the Kivi recursive-chat motivating example `μX. User→Agent:Utt.Agent→Skill:Sub.Skill→Agent:Resp.Agent→User:Reply.X` (all 3 roles iterate; Skill's body collapses to `?Sub.!Resp.X`), the "uniform-continuation choice" pattern (Agent decides whether to ack User; either way delivers `T` to Skill; Skill's projection cleanly merges to `?T.end`), JSON round-trip, `contains_var` shadowing correctness, and `is_safely_realizable` rejects the diverging choice. **Bug caught in vivo**: the first draft's `Rec` projection produced degenerate `μX.X` for a non-participating role; fix = non-participation rule (check `body_roles.contains(r)` before descending — return `End` if absent). The Kivi test initially failed because I'd constructed a choice the Skill couldn't observe — correctly rejected by the gate; lesson archived (the gate catches real protocol bugs, not just synthetic ones). **Cross-stack regression**: axon-frontend lib **535** (+18), axon-rs lib **2245** (re-export only). Zero regressions. **Scope deferred to a follow-up**: surface declaration (`protocol Name { … }` block in `.axon` source + IR + checker) — 41.h ships the algebra + the gate; enterprise wiring (one binding per role) reuses the existing §Fase 41.f WS surface unchanged. |
| **41.i** ✅ SHIPPED 2026-05-22 — **Fase 41 CLOSED** | **CI + fuzz + docs + release**: dedicated lane (duality/projection/credit fuzz + the deadlock-freedom invariants), adopter docs, coordinated release **axon-lang v2.3.0** + **axon-enterprise catch-up**. | ✅ **Coordinated release live cross-stack**. **axon-lang v2.3.0**: commit `12c06d3` + GitHub Release https://github.com/Bemarking/axon-lang/releases/tag/v2.3.0 + crates.io `axon-lang 2.3.0` + `axon-frontend 1.1.0` (additive: Polarity + multiparty + SealedRuntime + serde derives). Five cross-stack version files bumped via `bump-my-version minor` (pyproject + axon/__init__ + _bootstrap + axon-rs/Cargo.toml + cli smoke + frontend contract golden). Three tags pushed (`v2.3.0`, `rust-v2.3.0`, `axon-frontend-v1.1.0`). **axon-enterprise v2.1.0**: PR #49 merged commit `34f4624` + tag `enterprise/v2.1.0:refs/tags/v2.1.0` refspec-pushed + GitHub Release https://github.com/Bemarking/axon-enterprise/releases/tag/v2.1.0; 22 workspace crates bumped 2.0.1 → 2.1.0; workspace pin restored from 41.f-h path-override to `axon-lang = "=2.3.0"` (D1 Fase 40 invariant honored end-to-end); +1 missing re-export `CognitiveStateRepo` in lib.rs caught by the registry build (path-override builds had silently resolved it transitively). **Fuzz lane** (deterministic LCG, 50-seed corpus per surface): 6 axon-frontend surfaces (duality involutivity, connection-law symmetry, credit_analyse totality, polarity dual-symmetry, multiparty projection totality, two-role HYC soundness) + 2 axon-rs surfaces (sealed-runtime JSON round-trip, schema-mismatch rejection). **CI workflow** new `.github/workflows/fase_41_session_types.yml` with 7 parallel lanes (algebra / runtime / WS E2E / SSE E2E / fuzz-algebra / fuzz-runtime / D8 backwards-compat over Fase 33 SSE family). **Adopter doc** `docs/ADOPTER_SESSION_TYPES.md` (~520 LOC) — 12-section guide anchored to paper §3.1/3.2/3.5/4.2/4.4/5.1/5.2/6. **Migration doc** `docs/MIGRATION_v2.3.md` (~250 LOC) — 5 scenario-driven recipes (adopt `socket`, add credit, enable typed reconnect, expose as SSE, use multiparty algebra). **Cross-stack regression**: axon-frontend lib **535** (+18), axon-rs lib **2245** (no change), axon-enterprise-server lib **86**, rbac **17**, audit **24**, all §Fase 41.* E2E green (5 + 4 = 9), all §Fase 30/31/32/33 SSE family still green (D8 backwards-compat verified — D8 holds end-to-end). Zero regressions cross-stack at the release boundary. **Kivi single-image acceptance**: one Docker image `axon-enterprise-server` serves REST + SSE + WebSocket session-typed + typed reconnection on port 8080 with one auth gate, one audit chain, one tenant scope, one shield. **Fase 41 cycle CLOSED** — D1-D8 ratified, 9 sub-fases shipped, two coordinated releases (axon-lang v2.3.0 + axon-enterprise v2.1.0) live. |

## Relationship to Fase 33 (the dual)

Fase 33 made **SSE** (server→client monologue) a cognitive primitive. Fase 41 is
its **bidirectional dual completion**: the same stream theory, generalized to a
two-polarity session. Formally SSE is the downstream projection of the WebSocket
dialogue ($S_{\mathrm{SSE}} = \Pi_{\downarrow}(S_{\mathrm{WS}})$, §4.4 of the paper)
— so the implementation **reuses** the SSE machinery rather than duplicating it.
SSE typed the server's half of the conversation; Fase 41 types the whole dialogue.
