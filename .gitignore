# ──────────────────────────────────────────────────────────────────
# Python
# ──────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# Packaging / builds
build/
dist/
wheels/
*.egg-info/
*.egg
MANIFEST

# Virtualenvs
.venv/
venv/
env/
ENV/
.python-version

# Test / coverage / linters
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml
.tox/
.nox/

# ──────────────────────────────────────────────────────────────────
# Terraform / Infra
# ──────────────────────────────────────────────────────────────────
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
tfplan
tfplan*
crash.log
crash.*.log
# Secrets: keep *.tfvars.example but never commit real tfvars
*.tfvars
!*.tfvars.example
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# ──────────────────────────────────────────────────────────────────
# Docker / Kubernetes secrets
# ──────────────────────────────────────────────────────────────────
.env
.env.*
!.env.example
docker-compose.override.yml
kubeconfig
*.kubeconfig

# ──────────────────────────────────────────────────────────────────
# IDEs / editors / OS
# ──────────────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# ──────────────────────────────────────────────────────────────────
# Local agent / tooling workspaces
# ──────────────────────────────────────────────────────────────────
.claude/
.agent/
.axon-dev/

# ──────────────────────────────────────────────────────────────────
# Secrets (belt-and-suspenders)
# ──────────────────────────────────────────────────────────────────
*.pem
*.key
*.p12
*.pfx
credentials.json
secrets.yaml
secrets.yml
